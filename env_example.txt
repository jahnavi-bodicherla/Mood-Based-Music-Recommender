# MoodTunes — API Keys
# 1. Copy this to a file named exactly:  .env
# 2. Fill in your real keys
# 3. Save as UTF-8 (in Notepad: Save As > All Files > .env > Encoding: UTF-8)

# App secret — generate one: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=moodtunes_change_this_to_random_hex

# Spotify Web API (https://developer.spotify.com/dashboard)
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here
SPOTIFY_REDIRECT_URI=http://localhost:8501/callback

# YouTube Data API v3 (https://console.cloud.google.com)
YOUTUBE_API_KEY=your_youtube_api_key_here
